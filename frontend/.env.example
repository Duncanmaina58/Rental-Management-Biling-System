# Vite only exposes vars prefixed with VITE_ to client code.
# Currently unused since we proxy /api via vite.config.ts, but kept
# for any future direct API URL override (e.g. staging/prod builds).
VITE_API_BASE_URL=http://localhost:4000
